const Courses = () => {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
        background: "var(--red-primary-brand-color)",
      }}
    >
      courses
    </div>
  );
};

export default Courses;
